# /etc/qubes/openclaw.conf — OpenClaw dom0 configuration
#
# Which VM runs the OpenClaw proxy + gateway?
OPENCLAW_VM=visyble

# Ports (used by tunnel services and firewall)
# OPENCLAW_PROXY_PORT=32125
# OPENCLAW_GATEWAY_PORT=18789
